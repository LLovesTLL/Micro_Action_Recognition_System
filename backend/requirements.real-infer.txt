# Real model inference dependencies (VideoMambaPro backend)
# Use with Python 3.10 and torch 2.1.1/cu118 installed separately.

fastapi==0.116.1
uvicorn[standard]==0.35.0
python-multipart==0.0.20
pydantic==2.11.7
numpy==1.26.4
opencv-python-headless==4.12.0.88

# Common model-side runtime deps
einops==0.7.0
timm==0.4.12
av==11.0.0
decord==0.6.0

# Explainability and plotting (optional but recommended for Step 2)
captum==0.7.0
matplotlib==3.8.4
seaborn==0.13.2
